# v9.1.12 検証対象

## 追加した回帰試験

- 完成EXEの自己テストが `ValidateNearbyWashRecovery` を実行する。画像判定の不確定4種×洗浄/補充×操作状態6種、キャンセル前後、許可しない重大エラー、手持ち石0/不明/正数を試験する。
- 本番C# bridgeから実際の式を抽出してDOM試験を行う。洗浄と荷台の同時表示、ストレージだけ、無効状態、非表示、複数荷台、一般の「インベントリを開く」との区別、クリック0回を確認する。
- 新しいprobeは1回のJavaScript評価内で両ラベルを観測し、3サンプルと前後の3フレーム接続IDを検証する。片方ずつの表示を過去の別サンプルと合成しない。
- 既存の収納・補充・二重転送防止と洗浄再開の実報酬判定は変更しない。
- 既存のWindows実入力を使う合成画面試験、オーバーレイ描画、4幅の画面操作、完成EXEのGUIアップデーター互換試験を維持する。

試験結果は当該PRとWindows Actionsのログ・添付成果物に記録する。リリース前には署名済み配布物を旧版Updaterで実際に置換・検査・再起動し、設定が維持されることを確認する。

## 成功表示の区別

画像補正が成功した場合だけ既存の画像確認済み補正カウンターを増やす。画像補正が未確定でも操作範囲で続行する場合は「NEARBY_WASH_READY」、最後の石0から補充に進む場合は「NEARBY_REFILL_READY」として別記録する。これらは収納成功・石補充成功・次の洗浄成功の証拠ではない。各操作の実確認は既存フローが担当する。

## 実施済みのビルド検証

- Windows実行: https://github.com/kanzakideath/fivemstonefarm/actions/runs/34664614918
- 実装保存コミット: `be0c8e531cd5a84c49f83c646a07ca02771a1076`
- フルビルド、完成EXEの検査、同時ボタンDOM回帰、Windows合成画面への実W入力、実オーバーレイ、4幅の設定操作: 成功。
- コンソールなし検査（既定設定）: 終了0、3017ms。
- コンソールなし検査（カスタム設定）: 終了0、2871ms。
- リダイレクトあり検査（カスタム設定）: 終了0。
- コンソールなしUI smoke: 終了0、17479ms。
- 上記4起動条件で既存インストールファイルはすべて不変。

これらは保存済み成果物 `nearby-wash-evidence` の `SOURCE.txt`、`windows-validation.txt`、`artifacts/update-startup/results.json` に対応する。今回の文書追記は実行コードを変更しない。PR #7で同じ実装を読み取り専用CIにより再ビルドし、公開工程では署名済みの最終EXEを旧Updaterで実際に置換・検査・再起動する。公開後の証拠はPR #7の配信結果コメントに記録する。

## 未検証

合成画面・DOM・状態遷移試験は実FiveMの長時間プレイではない。実ゲームでの位置補正精度、通信遅延下の連続稼働、実荷台の収納/補充成功率は未測定。サーバー固有のUI・ラベルや車両の移動によって安全停止する場合がある。
