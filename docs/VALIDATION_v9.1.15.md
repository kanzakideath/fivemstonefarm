# v9.1.15 検証設計

- 完成EXEの自己テストがStationaryOnlyPolicyを実行。移動コマンド拒否、作業/石0時の準備完了、欠落1000回の待機判断、未知のエラーを拒否。
- 実際の `src/stationary-only.ahk` を含むAHKハーネスで、本番の待機ループ・荷台照合・復旧関数を実行。I/Oのみ合成。欠落後の自動復帰・実待機・タイマーによる中止・古い世代の結果拒否・台帳/レシートの維持・別荷台・未確定転送の再試行禁止を確認。
- 本番C#の実NUI式で3作業の同時表示と、クリック直前の荷台欠落を確認。単に過去の片方の表示を現在の表示と合成しない。
- アプリからのネイティブ移動・視点・ルートヘルパー入口拒否を契約試験。旧ヘルパーのコードは互換テスト用に残るがアプリから起動しない。
- UI4幅で荷台前設定、旧徒歩ボタン非表示、物理座標固定ではない説明、診断入口。
- 更新用署名・既存アップデーター互換、設定保持、保存・報酬・転送・二重送信防止の既存試験を維持。

合格した項目はCIの実結果を確認して記録する。ここに項目を書くだけで合格とはしない。実FiveMでの無操作・連続収納/補充・通信断を含む成功率は未測定。ゲーム側の座標固定や範囲外からの無移動復帰は実装しない。
