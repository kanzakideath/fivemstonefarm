{
  "name": "ai-miner-web-ui",
  "version": "8.0.1",
  "private": true,
  "description": "Offline Framework7 UI for AI Miner",
  "scripts": {
    "build": "node build.mjs",
    "check": "node --check src/app.js && node --check build.mjs && node --check test.mjs && node test.mjs"
  },
  "dependencies": {
    "framework7": "9.1.3"
  },
  "engines": {
    "node": ">=20"
  }
}
